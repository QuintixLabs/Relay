{
  "name": "relay",
  "version": "1.0.0",
  "description": "Minimal private power control app for Tailscale networks",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/dev.js",
    "start": "node src/server.js",
    "check": "node --check src/server.js",
    "lint": "eslint ."
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@eslint/js": "^9.24.0",
    "eslint": "^9.24.0",
    "globals": "^16.0.0"
  }
}
